# Optional Docker notes

Use an Ubuntu 22.04 ROS 2 Humble base image with Gazebo Classic and source `/opt/ros/humble/setup.bash` before building this workspace. GUI forwarding is environment-specific; native Ubuntu installation is recommended for Gazebo rendering.
